"""
mediadata provides functionality to manage media files and
manipulate their metadata.
"""

from mediadata.utils.version import get_version

VERSION = (0, 1, 0, "alpha", 1)

__version__ = get_version(VERSION)
